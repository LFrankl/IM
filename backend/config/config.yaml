server:
  port: 8080
  mode: debug        # debug / release

database:
  path: ./data/im.db

jwt:
  secret: "im-dev-secret-change-in-production"
  expire: 168h       # 7 天

upload:
  dir: ./data/uploads
  max_size: 10       # MB

cors:
  allow_origins:
    - "http://localhost:5173"
